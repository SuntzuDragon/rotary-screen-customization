# Build toolchain, pinned.
#
# A firmware build should be reproducible: the same tag must produce the same
# image months from now. Unpinned tooling also meant no pip cache in CI, since
# actions/setup-python needs a dependency file to hash.
platformio==6.2.0
esptool==5.3.1
